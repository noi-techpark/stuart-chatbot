# Subset of environment variables for standalone web development.
# For full-stack deployment, see the root .env.example instead.

BASIC_AUTH_USER=user
BASIC_AUTH_PASSWORD=password

PRESHARED_SECRET=suchsecret
BIND_IP=0.0.0.0
BIND_PORT=9001
